# atm-hermes-testbed — project plan / sprint index

Planning index for testbed sprints. Each sprint lands a doc under
`docs/sprints/<SPRINT-ID>.md` (frontmatter: `status`, `branch`, `worktree`)
and an entry here before closeout.

## Sprints

| Sprint | Status | Branch | Summary |
|---|---|---|---|
| TESTBED-SMOKE-SKILL-R1 | complete | feature/smoke-skill | Bake the `atm-smoke-test` hermes skill into the image; the container agent runs it on instruction and sends one sanitized PASS/FAIL report. Replaces step lists/tier coordination as the hermes integration test. |
